# The main job for the data-anonymization-databricks pyproject.
resources:
  jobs:
    template_job:
      name: template_job

      email_notifications:
        on_failure:
          - luijk.r@gmail.com

      tasks:
        - task_key: notebook_task
          notebook_task:
            notebook_path: ../notebooks/hello_revodata.py
